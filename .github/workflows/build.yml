name: Build RUNE Mobile APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Install MAUI Android workload
        run: dotnet workload install maui-android

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r27c

      - name: Clone llama.cpp
        run: git clone --depth 1 https://github.com/ggerganov/llama.cpp native-llama

      - name: Configure llama.cpp for Android
        run: |
          cmake -S native-llama -B native-llama/build-android \
            -DCMAKE_TOOLCHAIN_FILE=${{ steps.setup-ndk.outputs.ndk-path }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-26 \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=ON \
            -DLLAMA_CURL=OFF

      - name: Build llama.cpp for Android
        run: cmake --build native-llama/build-android --config Release -j 4

      - name: Copy native libraries into project
        run: |
          mkdir -p lib/arm64-v8a
          find native-llama/build-android -name "*.so" -exec cp {} lib/arm64-v8a/ \;
          ls -la lib/arm64-v8a/

      - name: Restore dependencies
        run: dotnet restore RuneMobile.csproj

      - name: Build APK
        run: dotnet build RuneMobile.csproj -f net9.0-android -c Release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: RUNE-Mobile-APK
          path: bin/Release/net9.0-android/*Signed.apk
